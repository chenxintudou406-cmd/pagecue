# 页知 3.2.5：评价确认与用户设置分区

## 评价

- 赞与踩互斥选择，点击后立即显示实色背景、描边、阴影和勾选标记。
- 当前成员对当前提醒版本的选择保存到服务端，重新打开卡片或更换设备后仍可恢复。
- 离线点击会先保存在扩展事件队列和本地卡片状态，恢复连接后补传。
- 后台继续保留每次评价事件，并保留每位成员对每条提醒的当前评价。

## 设置页

设置页拆分为四个独立玻璃卡片：

1. 打开方式
2. 账号与权限
3. 页面行为
4. 连接与版本

每个分区具有编号、标题、说明、边框和独立留白，原有功能入口与元素 ID 保持不变。
