{
  "name": "pagecue",
  "version": "3.1.2",
  "private": true,
  "description": "Context-aware notes, team reminders, and quick tools for Chrome and Edge.",
  "scripts": {
    "start": "node server.js",
    "test": "node scripts/build-extensions.js && node --test tests/*.test.js",
    "build:extensions": "node scripts/build-extensions.js",
    "migrate:v3": "node scripts/migrate-v3.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "web-push": "^3.6.7"
  }
}
